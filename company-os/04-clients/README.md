# Clients

Move won deals from `03-prospects/won/<slug>` into `active/<slug>` or mirror structure here. Archive to `past/` when engagement ends.

## Folder convention

Mirror the prospect folder layout (`company-profile.md`, `workflow-analysis.md`, etc.) and add:

- `support-log.md`
- `roadmap-shared.md` (what the client sees)
