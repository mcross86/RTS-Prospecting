# Reusable components

When you extract UI or logic from a client build, note the path to the **git repo** and what is safe to reuse (no client branding or private data).

| Component | Repo / path | Used in |
|-----------|-------------|---------|
| | | |
